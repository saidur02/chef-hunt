
const MainSection = ({chef}) => {
   
const {name,experience,likes,img} = chef;
    return (
        <Container>
           <h2>{name}</h2>
        </Container>
    );
};

export default MainSection;